package com.EKEY.Board.ChessFigures.Movement;
// TODO: DELETE
@Deprecated
public class WhitePawnMovement{
	/*
	public WhitePawnMovement(Figure figure) {
		super(figure);
	}
	
	@Override
	public void renderOptions(Graphics g) {
		Color c = new Color(0f, 0, 0f, 0.5f);
		g.setColor(c);
		Camera cam = Camera.getInstance();
		
		System.out.println("LOC X: " + linkedFigure.getTileX() + " Y:" + linkedFigure.getTileY());
		
		BoardTile stand = DataShare.BOARD.getTileByLoc(linkedFigure.getTileY(), linkedFigure.getTileX());
		
		BoardTile standF = DataShare.BOARD.getTileByLoc(stand.getTileY() - 1, stand.getTileX() );
		
		BoardTile standF2 = DataShare.BOARD.getTileByLoc(standF.getTileY() - 1, standF.getTileX() );
		
		g.fillOval(standF.getX() - cam.getCameraX() + 47, standF.getY() + 47 - cam.getCameraY(), 32, 32);
		g.fillOval(standF2.getX() - cam.getCameraX() + 47, standF2.getY() + 47 - cam.getCameraY(), 32, 32);
		
		
	}

	@Override
	public void moveFigure(BoardTile tile) {
		// TODO Auto-generated method stub
		
	}
*/
}
